<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"/>
    <title>Document</title>
</head>
<body>
    <div class="container mt-5">
        <h1 id="driverName"></h1>
        <h1 id="locationDetail">{{number}}</h1>
    </div>
    <script>
      const resultFn = async () => {
        result = await fetch("http://127.0.0.1:8000/track-order", {
            method: "POST", // *GET, POST, PUT, DELETE, etc.
            mode: "cors", // no-cors, *cors, same-origin
            cache: "no-cache", // *default, no-cache, reload, force-cache, only-if-cached
            credentials: "same-origin", // include, *same-origin, omit
            headers: {
              "Content-Type": "application/json",
              // 'Content-Type': 'application/x-www-form-urlencoded',
            }
            });
        const data = await result.json();
        console.log(data)
        //document.getElementById('driverName').innerHTML = data;
        //document.getElementById('locationDetail').innerHTML = data;
      }
      resultFn();
    </script>
</body>
</html>